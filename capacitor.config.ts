import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hulululu.sample',
  appName: 'GIGA PLAYS',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
